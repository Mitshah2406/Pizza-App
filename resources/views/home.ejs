  <!-- hero -->
  <section class="hero py-16">
    <div class="container mx-auto flex items-center justify-between">
      <div class="w-1/2">
        <em class="text-lg">Are You Hungry?</em>
        <h1 class="text-6xl font-bold">Don't Wait</h1>
        <button class="btn-primary py-2 px-6 rounded-full text-white font-bold mt-4">Order Now</button>
      </div>
      <div class="w-1/2"><img src="img/hero-pizza.png" alt=""></div>
    </div>
  </section>
</body>
<!-- menu -->
<section class="menu container mx-auto py-8">
  <h1 class="text-xl font-bold mb-8">All Pizzas</h1> 
  <div class="grid grid-cols-4 col-gap-12 row-gap-16">
    <% pizzas.forEach(pizza => { %>
      <div>
        <img class="h-40 mb-4 mx-auto"src="/img/<%-pizza.image%>" alt="pizza">
        <div class="text-center">
          <h2 class="mb-4 text-lg"><%-pizza.name%></h2>
          <span class="size py-1 px-4 mt-6 rounded-full uppercase text-xs"><%-pizza.size%></span>
          <div class="flex items-center justify-around">
            <span class="font-bold text-lg">₹<%-pizza.price%></span>
            <button data-pizza = "<%= JSON.stringify(pizza) %>" class="add-to-cart py-1 px-6 rounded-full flex items-center font-bold">
              <span>+</span>
              <span class="ml-4">Add</span>
            </button>
          </div>
        </div>
      </div>
    <% }) %>
 
  </div>
</section>